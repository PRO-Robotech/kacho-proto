// Copyright (c) PRO-Robotech
// SPDX-License-Identifier: BUSL-1.1

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: kacho/cloud/apigateway/v1/internal_authz_cache_service.proto

// Package kacho.cloud.apigateway.v1 — cluster-internal RPCs of api-gateway.
// All services in this package are registered ONLY on the api-gateway
// internal mTLS listener (port 9091). They are NEVER exposed on the
// external TLS REST mux.

package apigatewayv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	emptypb "google.golang.org/protobuf/types/known/emptypb"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	InternalAuthzCacheService_InvalidateSubject_FullMethodName = "/kacho.cloud.apigateway.v1.InternalAuthzCacheService/InvalidateSubject"
)

// InternalAuthzCacheServiceClient is the client API for InternalAuthzCacheService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// InternalAuthzCacheService — cluster-internal cache control for api-gateway's
// authz-decision cache.
//
// Caller: kacho-iam `SubjectChangeApplier` (the drainer-applier for
// `kacho_iam.subject_change_outbox`), invoked once per drained row immediately
// after the row was claimed (atomically) by the push-drainer.
//
// Callee: api-gateway, on its internal mTLS gRPC listener (port 9091).
// Not registered on the external TLS endpoint.
//
// Purpose: replace the poll-loop's whole-cache flush
// (`AuthzMiddleware.InvalidateCache`) with per-subject invalidation
// (`decisionCache.InvalidateSubject(subject) int`) — drops only entries
// scoped to the changed subject. The poll-loop remains as a 30s safety net
// for the push-path.
//
// Latency promise: ≥ 1 api-gateway replica converges (< 1s) on revoke commit
// via this RPC. All HA-replicas converge within ≤ 30s via the safety-net
// poll-loop. (Fanout to all replicas in < 1s is out of scope.)
type InternalAuthzCacheServiceClient interface {
	// InvalidateSubject drops decision-cache entries scoped to the given
	// subject (FGA prefix). Idempotent: a subject with no cached entries
	// returns codes.NotFound, which the drainer maps to
	// drainer.ErrAlreadyApplied so the outbox row is marked sent_at and
	// never re-tried.
	//
	// The response is purposefully empty — subjects-affected count is
	// reported via metric `apigw.authz.cache.invalidate.entries` (labels:
	// event_type, subject_type) instead of the RPC reply.
	InvalidateSubject(ctx context.Context, in *InvalidateSubjectRequest, opts ...grpc.CallOption) (*emptypb.Empty, error)
}

type internalAuthzCacheServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewInternalAuthzCacheServiceClient(cc grpc.ClientConnInterface) InternalAuthzCacheServiceClient {
	return &internalAuthzCacheServiceClient{cc}
}

func (c *internalAuthzCacheServiceClient) InvalidateSubject(ctx context.Context, in *InvalidateSubjectRequest, opts ...grpc.CallOption) (*emptypb.Empty, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(emptypb.Empty)
	err := c.cc.Invoke(ctx, InternalAuthzCacheService_InvalidateSubject_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// InternalAuthzCacheServiceServer is the server API for InternalAuthzCacheService service.
// All implementations must embed UnimplementedInternalAuthzCacheServiceServer
// for forward compatibility.
//
// InternalAuthzCacheService — cluster-internal cache control for api-gateway's
// authz-decision cache.
//
// Caller: kacho-iam `SubjectChangeApplier` (the drainer-applier for
// `kacho_iam.subject_change_outbox`), invoked once per drained row immediately
// after the row was claimed (atomically) by the push-drainer.
//
// Callee: api-gateway, on its internal mTLS gRPC listener (port 9091).
// Not registered on the external TLS endpoint.
//
// Purpose: replace the poll-loop's whole-cache flush
// (`AuthzMiddleware.InvalidateCache`) with per-subject invalidation
// (`decisionCache.InvalidateSubject(subject) int`) — drops only entries
// scoped to the changed subject. The poll-loop remains as a 30s safety net
// for the push-path.
//
// Latency promise: ≥ 1 api-gateway replica converges (< 1s) on revoke commit
// via this RPC. All HA-replicas converge within ≤ 30s via the safety-net
// poll-loop. (Fanout to all replicas in < 1s is out of scope.)
type InternalAuthzCacheServiceServer interface {
	// InvalidateSubject drops decision-cache entries scoped to the given
	// subject (FGA prefix). Idempotent: a subject with no cached entries
	// returns codes.NotFound, which the drainer maps to
	// drainer.ErrAlreadyApplied so the outbox row is marked sent_at and
	// never re-tried.
	//
	// The response is purposefully empty — subjects-affected count is
	// reported via metric `apigw.authz.cache.invalidate.entries` (labels:
	// event_type, subject_type) instead of the RPC reply.
	InvalidateSubject(context.Context, *InvalidateSubjectRequest) (*emptypb.Empty, error)
	mustEmbedUnimplementedInternalAuthzCacheServiceServer()
}

// UnimplementedInternalAuthzCacheServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedInternalAuthzCacheServiceServer struct{}

func (UnimplementedInternalAuthzCacheServiceServer) InvalidateSubject(context.Context, *InvalidateSubjectRequest) (*emptypb.Empty, error) {
	return nil, status.Error(codes.Unimplemented, "method InvalidateSubject not implemented")
}
func (UnimplementedInternalAuthzCacheServiceServer) mustEmbedUnimplementedInternalAuthzCacheServiceServer() {
}
func (UnimplementedInternalAuthzCacheServiceServer) testEmbeddedByValue() {}

// UnsafeInternalAuthzCacheServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to InternalAuthzCacheServiceServer will
// result in compilation errors.
type UnsafeInternalAuthzCacheServiceServer interface {
	mustEmbedUnimplementedInternalAuthzCacheServiceServer()
}

func RegisterInternalAuthzCacheServiceServer(s grpc.ServiceRegistrar, srv InternalAuthzCacheServiceServer) {
	// If the following call panics, it indicates UnimplementedInternalAuthzCacheServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&InternalAuthzCacheService_ServiceDesc, srv)
}

func _InternalAuthzCacheService_InvalidateSubject_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(InvalidateSubjectRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InternalAuthzCacheServiceServer).InvalidateSubject(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InternalAuthzCacheService_InvalidateSubject_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InternalAuthzCacheServiceServer).InvalidateSubject(ctx, req.(*InvalidateSubjectRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// InternalAuthzCacheService_ServiceDesc is the grpc.ServiceDesc for InternalAuthzCacheService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var InternalAuthzCacheService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "kacho.cloud.apigateway.v1.InternalAuthzCacheService",
	HandlerType: (*InternalAuthzCacheServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "InvalidateSubject",
			Handler:    _InternalAuthzCacheService_InvalidateSubject_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "kacho/cloud/apigateway/v1/internal_authz_cache_service.proto",
}
